第一层：基础配置和数据结构
    │
    ├── 1. shm_conf.h/cpp     ← 配置类，定义 Segment 的大小规则
    ├── 2. state.h/cpp         ← Segment 的状态管理（引用计数、序列号）
    └── 3. block.h/cpp         ← Block 结构（消息的元数据 + 读写锁）
    
第二层：核心 Segment 实现
    │
    ├── 4. segment.h/cpp       ← 核心！管理共享内存的读写
    ├── 5. posix_segment.h/cpp ← POSIX 共享内存实现
    └── 6. xsi_segment.h/cpp   ← System V 共享内存实现
    
第三层：通知机制
    │
    ├── 7. readable_info.h/cpp ← 可读消息的信息结构
    ├── 8. notifier_base.h     ← 通知器基类接口
    ├── 9. condition_notifier.h/cpp  ← 条件变量通知
    └── 10. multicast_notifier.h/cpp ← 组播通知
    
第四层：工厂类
    │
    ├── 11. segment_factory.h/cpp   ← 创建 Segment 的工厂
    └── 12. notifier_factory.h/cpp  ← 创建 Notifier 的工厂


先理解内存布局：看你之前分享的图，理解 State → Block → Buffer 的结构
读 shm_conf：理解配置是如何根据消息大小计算的
读 segment.h/cpp：这是核心，重点看：
AcquireBlockToWrite() - 获取一个可写的 Block
ReleaseWrittenBlock() - 释放写锁
AcquireBlockToRead() - 获取可读 Block
ReleaseReadBlock() - 释放读锁
最后看通知机制：理解写入后如何通知读者